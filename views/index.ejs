<!DOCTYPE html>
<html lang="en">

  <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link href="/stylesheets/bootstrap.css" rel="stylesheet">
      <link href="/stylesheets/custom.css" rel="stylesheet">

      <title>Share Your Knowledge</title>
  </head>


  <body>
  <div class="custom_web">
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <a class="navbar-brand" href="/">Share Your Knowledge</a>

        </div>
        <div class="navbar-collapse collapse">
          
          <form class="navbar-form navbar-right">
           
            <% if (!userName) { %>
                <a class="btn btn-success" href="/login/google">Sign In with Google</a>
            <% } else { %>
                <a class="navbar-brand" href="/profile"><%= userName %></a>
                <a class="btn btn-success" href="/logout">Logout</a> 
            <% } %>
            <!--<button type="submit" class="btn btn-success">Sign in</button>-->
            <!--<a class="btn btn-success" href="/users/new">Create an account</a>-->
          </form>

          <form method = "post" action="/search_public" class="navbar-form navbar-right">
            <div class="input-append">
              <input class="search-query input-medium"  name="search_query" type="text" placeholder="Type for searching" >
              <button type = "submit "class="btn btn-success" type="button">Search<i class="icon-search"></i></button>
            </div>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </div>

    <hr>
    <div>
      <%if(render_body == "main"){%>
        <%- include main.ejs %>
      <%}%>
      <%if(render_body == "about"){%>
        <%- include about.ejs %>
      <%}%>
      <%if(render_body == "profile"){%>
        <%- include profile.ejs %>
      <%}%>
      <%if(render_body == "upload"){%>
        <%- include upload.ejs %>
      <%}%>
      
    </div>


    <hr>

    <footer>
      <p>&copy; Design of Dynamic Web Systems 2013</p>
    </footer>


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
  </div>
  </body>
</html>
